{
  "name": "budgettracer-plaid-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev --local",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:production": "wrangler deploy --env production",
    "deploy:production-owned": "wrangler deploy --env production-owned",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
